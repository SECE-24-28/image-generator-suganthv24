[project]
name = "image-generator-suganthv24"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "accelerate",
    "diffusers",
    "matplotlib",
    "safetensors",
    "transformers>=4.46.3",
]